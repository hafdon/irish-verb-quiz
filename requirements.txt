# requirements.txt
# Example:
# tkinter is part of the Python standard library, so it doesn't need to be listed.
# If using external packages, list them here, e.g.,
# requests
# numpy

appdirs~=1.4.4
deepdiff~=8.0.1
requests~=2.32.3
playsound~=1.3.0